# Frontend vars
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_DEFAULT_ADMIN_EMAIL=
# Optional: force password recovery redirect URL
# Must be present in Supabase Auth > Redirect URLs
VITE_AUTH_REDIRECT_URL=
VITE_AVATARS_BUCKET=

# Server-side only (not used by frontend directly)
# Supabase Service Role Key (keep it secret; only for Edge Functions)
SUPABASE_SERVICE_ROLE_KEY=
